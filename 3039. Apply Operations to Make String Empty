/* BRUTE FORCE TLE */
class Solution {
    public String lastNonEmptyString(String s){
        int el=s.length();
        StringBuilder sb = new StringBuilder(s);
        String ans=s;
        while(sb.length()!=0){
            ans = sb.toString();
            int count[] = new int[26];
            for(int i=0;i<sb.length();){
                if(count[sb.charAt(i)-'a']<1){
                    count[sb.charAt(i)-'a']++;
                    sb.deleteCharAt(i);
                }
                else i++;   
            }
        }
        return ans;
    }
}

/*
LOGIC---
Just do what the question says.
*/
